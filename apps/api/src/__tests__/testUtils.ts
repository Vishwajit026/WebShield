// ── Vitest config inline (no extra config file needed) ─────────────────────
// Tests can be run with: pnpm test
// Individual files: pnpm vitest run src/__tests__/auth.register.test.ts

import { vi, describe, it, expect, beforeEach, afterEach } from 'vitest';

export { vi, describe, it, expect, beforeEach, afterEach };
